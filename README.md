## Configure OpenClaw (fka clawdbots) with AISA's unified API

### Option 1: When you initially set up an OpenClaw bot:



### Option 2: When you want to replace the model provider in an existing OpenClaw bot:

